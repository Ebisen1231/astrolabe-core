# 将来バックログ

現在のマイルストーンで意図的に実装しないもの。着手する前に AGENTS.md の範囲確認をすること。

- **Hacker News 収集**(設計書§6.1 手順1)— hnrss.org のRSSフィードをソースに追加する形が最小
- **精読・下読み工程**(設計書§6.1 手順4)— 一次選別上位5〜10本の本文を mini で要点抽出してから統合に渡す
- embeddings による重複排除の強化(§6.1 手順2。現状はタイトル正規化+既出判定のみ)
- RSSレスポンスのローカルキャッシュ(現状キャッシュはarXivのみ)
- 概念名の表記ゆれ統合(例: 「リランキング」と「リランキング(Reranking)」が別ノードになりうる)
- README(公開リポジトリの体裁を整える)
- 選別済みアイテムの既出キャッシュ(初回実運用の観測: RSS由来の同一アイテムが毎朝再スコアリングされる。現状 mini 22%/日で枠内だが、報告に引用されなかったアイテムも triage 済みとして記録すれば約半減できる見込み)
- interests の時間減衰・selectedによる重み強化アルゴリズム(M3以降。現状は既知概念と直近のselected/dismissedを決定的にプロンプトへ注入するだけ)
- **fixtureデモのVercel公開**(M2ではローカルfixture表示まで。公開は別途プライバシーと運用を確認してから行う)
- **実データのリモート閲覧**(M3のSupabase移行・認証導入と同時に扱う。M2はローカル読込のみ)
- デプロイ先の再評価(AWS等) — 具体的動機(常駐エージェント化・GPU/自前モデル実験・AWS経験の獲得)が出た時点で検討する。移行容易性は担保済み: coreは素のPythonパッケージ、eventsから全再導出可能、ストレージは抽象化済み。M4自己改修ループの学習タスク候補
